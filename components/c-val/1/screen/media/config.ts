/**
 * Maximum number of repeated video tiles rendered by the media screen.
 * Change this single value when testing a different multiplicity ceiling.
 */
export const C_VAL_MEDIA_MAX_CELLS = 400;

/** Number of repeated tiles contributed by each percentage-point move. */
export const C_VAL_MEDIA_CELLS_PER_PERCENT = 1;
