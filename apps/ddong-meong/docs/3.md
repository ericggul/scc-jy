# ddong-meong/3 — 2026-08-10

## Trial

Fork `ddong-meong/2` into a new, protected working variant before testing more
diverse media-art treatments for the accumulation background. This first `/3`
state is a control, not a new visual result.

## Routes

- `/ddong-meong/3`
- `/ddong-meong/3/main`
- `/ddong-meong/3/dummy`
- `/ddong-meong/3/letting-go`
- `/ddong-meong/3/waiting-body`
- `/ddong-meong/3/downward-breath`
- `/ddong-meong/3/private-room`
- `/ddong-meong/3/lighter-moment`
- `/ddong-meong/3/screen`
- `/ddong-meong/3/testing` → `/ddong-meong/3/testing/original`

`/ddong-meong` now opens the working `/3` route. Variants `1` and `2` remain
addressable archives.

## Changed variable

The fork receives its own public media paths, soundtrack DOM ID, nickname
storage key, DOM input ID, routes, React component names, socket event prefix,
socket room and server experiment ID. Its current material trial changes the
reader relation from scrolling text to pressing a spatial falling-material
origin; the details are preserved in [the interaction record](./3-interaction-drop-test.md).

The component family is also re-owned by role without changing behavior:

```text
components/ddong-meong/3/
  mobile/   # entry, list, content, reader, background, soundtrack, shell
  screen/   # exhibition screen and its client socket transport
  model/    # shared content and abstract session types
  design-system/
```

## Retained invariants

- every mobile screen, copy string, timing, layout and typography;
- all six content scripts and their immutable accumulation profiles;
- prelude, soundtrack behavior, automatic reading movement, timer bar and
  production flush sequence;
- the Three.js/WebGL renderer, seeded emission rhythm, shader code, profile
  validation and dynamic-viewport handling;
- the exhibition layout and its current socket-only skeleton;
- identical copies of the seven `/2` public media assets under `/public/ddong-meong/3`.

The working mobile, screen, and background-lab surfaces also suppress selection
and browser zoom gestures so their screen-scale interaction remains fixed. The
legacy `mobile copy` remains an untouched reference.

## Static result

`/3` has an independent route and socket namespace while preserving the exact
`/2` media-art baseline. No browser observation is claimed by this record.

## Unresolved question

Which one bounded material relation should differ first from this control:
the falling topology, the temporal rhythm, the accumulation field, or the
relationship between the reading text and the background?

The active follow-on is the preserved
[spatial interaction and background lab](./3-interaction-drop-test.md).
