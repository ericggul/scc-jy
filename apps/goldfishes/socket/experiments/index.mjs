// Goldfishes has no Socket.IO experiment yet. Keep this app-owned public entry
// empty until a concrete experiment defines its event prefix, room, and state.
export const goldfishesExperiments = Object.freeze([]);
