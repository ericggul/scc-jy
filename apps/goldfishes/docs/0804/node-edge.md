# Goldfishes 0804/node-edge

Date: 2026-08-04 · Route: `/0804/node-edge` · **Entropy-generated 3D topology as a revealed attraction field.**

The participant begins with the school alone and click/drags a responsive screen-space brush to cumulatively reveal nearby latent nodes; an edge appears only when both endpoints are active. Nodes and edges ease/grow into visibility through instance matrices only. Active nodes are the model targets, so the topology is not decoration. Escape clears manual reveal; `Field > show all` temporarily reveals everything without discarding it; reduced motion resolves immediately. Alt/right-drag orbits and wheel zooms from an oblique reset view. No labels, metric panels, grids, captions, or other wrapper are added.

On page load `crypto.getRandomValues` generates 240–520 nodes by viewport area, a perturbed-superellipse distribution reaching within roughly 1.2% of viewport edges, a deduplicated Bowyer–Watson neighbour graph, and a continuous normalized elevation field (seven influence centres plus three phased waves). Resize reprojects the same field; reload regenerates it. Node coordinates, elevation, and radius feed rendering, hit/reveal logic, media placement, and targets.

This fork owns a 3-axis CPU school (`x/y/z`, `vx/vy/vz`): its field `x/y` map to world X/Z and model `z` to elevation; it uses full-3D alignment/cohesion/separation and 27-bucket spatial-hash lookup, 3D overlap resolution, spherical target circulation, and protected-core response that projects to the nearest sphere surface and reflects only inward velocity. Vertical bounds derive from elevation; fish orient from actual 3D velocity. `Motion > depth` changes model target spread, not a renderer offset. Instanced cylinders/icosahedra add two network draw calls; an invisible floor is solely for pointer ray-casting. The directory owns all implementation and imports no other Goldfishes experiment.

**Record.** HTTPS review on 2026-08-04 at 1470×863 found different topology/height/school distributions after reload, edge-to-edge initial coverage, orbit-visible separation, no console errors, three full-viewport canvases, TypeScript, and ESLint success; it is not an FPS claim. A later isolated model run (36 fish, 480 frames at 60 Hz, one protected node, overlap on) kept all state finite: elevation span 72.14, 30 fish with `|vz| > 1`, nearest node distance 51.51 (radius 21), and nearest pair distance 36 (configured collision distance). That static/model evidence does not claim visual review of the later 3D-motion or progressive-reveal changes.
