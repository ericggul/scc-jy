# PC / 0908 / variations

`/pc/0908/variations`: a standalone copy of the default phone field. The bottom switch selects flat `colours`, a large technology `keywords` surface, or `keyword + sentence`; a local template ID and surface index choose the sentence. Surfaces advance only while invisible. Count, rows, size, margin, presence, and speed remain testable.
