# Stock 3 — Bloomberg workstation reconstruction

`/stock/3` is an accepted, reference-faithful Bloomberg-workstation study, not a generic dark finance dashboard. The supplied reference image is authoritative; request it again if its machine-local copy is unavailable. Preserve the composition, density, terminal typography, irregular deterministic charts, common symbol selection, and all records at every breakpoint.

## Wide-screen contract

The 100×100 grid has 2px gaps/padding: left `1/29`, middle `29/59`, right `59/101`. Rows: clocks `1/22`, hot lists `22/63`, story `63/101`; releases `1/18`, sovereign `18/65`, lower-middle `65/101`; matrix `1/53`, macro `53/73`, news/chart `73/101`. Lower splits: middle `29/45/59`, right `59/79/101`.

Required contents: 4 clocks; 20 Hot Up/Down tiles (5×4); a category, headline, four paragraphs + quotation, source and 2-series story chart; 5 releases; 20 sovereign rows; two breadth charts; 18 S&P rows (6/6/6); 28 matrix rows (7/9/12, sparkline, 7 heat cells, 52W marker, endpoint); 8 macro cards (price/change/H-L/open/net/volume/range/8 bars/sparkline/bid-ask); 14 news rows; 4-line relative chart. The arrays in `model/workstation-data.ts` own stable IDs.

At >1200px the mosaic is `100dvh` without scrolling. At 721–1200px panels become paired columns; below 720px they are single column; below 431px tiles become 2×10 and macro cards 2×4. The matrix retains a 900px inspectable width on narrow screens. Never hide or clip records; recalculate panel heights when counts change.

## Interaction and visual invariants

One `symbol` state is updated by command input, return tiles, sovereign and active rows, matrix rows, and macro cards; it feeds the security label and relative chart. Use accessible overlays for macro cards without nesting flow landmarks inside a button.

Use compact tabular monospace (`clamp(10px, min(0.7vw, 1.18vh), 14px)` base), right-aligned numbers, terminal rules, orange functional labels, green/red direction, cyan selection, purple comparison. No rounded cards, glow, gradients, faux-live badges, or ornamental metrics. Charts use seeded cumulative drift/noise/impulses—not `Math.random()` or sine waves—to keep hydration and screenshots stable.

## Architecture, history, verification

`components/dashboard/stock/3/model/workstation-data.ts` owns records/series; `screen/workstation.tsx` owns composition and selection; `screen/workstation.module.css` owns geometry. Keep routes thin.

Useful failures: an outer-grid-only clone was rejected as sparse; adding rows without height crushed text; a sine-chart recipe looked synthetic; delayed CSS on already-updated DOM was not a sequential information cascade. If live work is revisited, define visible tick magnitude and simulated-time mapping first, commit panels in causal order from separate current/next frames, and derive news from the same catalyst as market movement.

Static checks may use scoped lint and `pnpm exec tsc --noEmit`; do not start/build a server. Visual comparison requires explicit authorization and the already-running HTTPS route. Confirm the listed counts, readable columns, shared selection, stable React keys, chart visibility, and record retention. A previous unrelated missing `components/sns/youtube/1/screen` module blocked repository-wide TypeScript at this handoff.
