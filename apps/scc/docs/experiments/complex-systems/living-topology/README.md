# Living topology

This route studies a changing graph through its actual birth, relation, retraction and topology state. Preserve stable entity IDs, model-owned topology, deterministic model checks, and direct manipulation. The rendering must reveal changes in connection/occupancy rather than decorate a fixed graph; no inherited complex-systems palette, dashboard counters, or presentation fields in the model.

Keep the route-specific source/material ledger alongside its local assets. Any new mechanism must be a bounded fork: state the changed rule, retained topology/interaction invariants, observed result, and unresolved question.
