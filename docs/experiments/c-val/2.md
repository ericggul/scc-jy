# C-VAL 2

> Status on 2026-08-04: active implementation of one fixed bodily chain,
> awaiting real-phone verification. C-VAL 1 remains the frozen reference.

This is a bounded replacement trial after the multi-axis sum failed both
required qualities: it was less dynamic than C-VAL 1 and still did not let a
person form a stable angle-to-rise/fall expectation. The failure and its
do-not-repeat rules are recorded in
[`2-interaction-failure-review.md`](./2-interaction-failure-review.md).

## Today's exact task

C-VAL 2 must let an ordinary person discover a repeatable relation between a
directional phone movement and the market price:

```text
move the phone this way -> price rises
move it the opposite way -> price falls
move it around a balancing direction -> price stays relatively still
```

V/A/L are necessary intermediate market conditions. They are not GUI controls,
three modes to select, or target numbers the participant must precisely learn.
The participant learns the bodily `phone movement -> price` relation by trying
directions and watching the actual market response.

The complete active chain is:

```text
one human phone movement
  -> one fixed calibrated forward/back beta angle
  -> V/A/L change together
  -> fixed V/A/L market direction and strength
  -> ordinary market agents change order sides and valuations
  -> FIFO executions
  -> actual displayed price
  -> surrounding social screens
```

There is no second input channel and no direct assignment of displayed price.
Price has no configured ceiling. Its only bound is a one-tick positive floor
of `0.01`, because a traded equity price cannot cross below zero.

## Non-negotiable interaction rules

1. The only participant input is moving, shaking, or rotating the phone.
2. After browser-required motion permission, there is no V/A/L selector,
   slider, mode button, gesture tutorial, BUY/SELL control, or commit action.
3. The sensor coordinate system is fixed. It does not learn a private axis from
   the first gesture.
4. One fixed forward/back phone angle changes V/A/L together by the same
   equations on every sample.
5. V/A/L immediately alter one existing agent-and-order-book market path.
6. No `pressure`, privileged operator, hidden gesture classifier, RMS window,
   cadence detector, dwell gate, or pose-hold condition exists.
7. The mobile screen gives feedback; it does not become a second control
   surface.

## Lifecycle

Before meaningful movement:

- phase is `waiting`;
- price is exactly `100`;
- there are no participants, resting orders, executions, turnover, or depth;
- the random market process does not advance.

The first orientation event establishes a local zero. A two-degree beta
difference from that zero is the only sensor-noise boundary. Crossing it
activates the market once; the fixed beta coordinate remains the only source
of market direction. Reset from the controller returns the complete
system to the dormant state and asks connected mobiles to establish a new zero.

After activation the market continues running. When current phone input returns
toward its balanced pose or disappears, V/A/L return toward neutral and the
directional bias diminishes rather than inventing a new gesture state.

## Fixed phone-angle-to-V/A/L mapping

Independent alpha/beta/gamma control was rejected because a person could not
form a stable expectation from three coupled physical axes. C-VAL 2 now uses
one fixed, visible forward/back pitch angle (`beta`) as its bodily coordinate:

```text
direction = clamp(beta / 35 degrees, -1, 1)
intensity = abs(direction)

V = 0.5 + 0.5 * intensity
A = 0.5 + 0.5 * direction
L = 0.5 - 0.5 * intensity
```

Forward and backward angles therefore produce equal intensity and fragility but
opposite directional activity. At the balanced pose all three are `50`. At
either 35-degree extreme, V becomes `100`, L becomes `0`, and A becomes either
`100` or `0`. Nothing is selected or latched. Alpha and gamma cannot silently
change market direction.

## Fixed V/A/L-to-market mapping

V, A, and L keep their ordinary market roles:

- V controls information and valuation dispersion and quote risk;
- A controls the rate of agent orders;
- L controls passive supply, replenishment, spread, depth, and price impact.

For the bodily direction to be learnable, the same intermediate values also
produce one fixed signed condition direction:

```text
signed activity = 2 * (A - 0.5)
volatility gain = 0.25 + 0.75 * V
liquidity gain  = 1 - 0.75 * L
market direction = signed activity * volatility gain * liquidity gain
```

This is not an additional participant parameter. It is a deterministic market
consequence of the current V/A/L state:

- A above `50` biases valuation changes and ordinary agent order sides upward;
- A below `50` biases them downward with symmetric strength;
- A at `50` cancels toward stillness;
- higher V strengthens either direction;
- higher L damps either direction and lower L exposes larger price impact.

Strong direction suppresses unrelated random innovation, so opposite repeated
phone directions remain perceptually opposite instead of being hidden by
noise. All price changes still come from real FIFO executions by the existing
participant types. There is no privileged human order and no direct mutation of
the displayed price.

## What the participant should learn

The participant should not need to name alpha, beta, gamma, V, A, or L. Through
repetition they should form an embodied expectation:

- one region/direction of motion makes executed price rise;
- its opposite makes executed price fall;
- the balanced forward/back angle produces little directional drive;
- motion that raises liquidity makes the same drive less destructive;
- motion that lowers liquidity makes it more destructive.

The mapping may initially be abstract. It must nevertheless be fixed,
repeatable, immediate, and learnable by a human through action and consequence.

## Mobile feedback screen

The mobile has no post-permission control widgets.

- one horizontal line mirrors the only operative forward/back beta angle;
- the bottom row exposes current V/A/L intermediate values;
- the center shows the actual last executed price;
- `RISING`, `FALLING`, `STILL`, or `WAITING` comes from the actual one-second
  execution window, not from predicted direction.

This closes the learning loop on the same device: bodily motion, intermediate
conditions, and realized price are visible together. The screen never asks the
person to tap V/A/L or choose how the movement should be interpreted.

## Multiple humans

Every phone uses the same mapping. Current contributions use one transparent
rule: an equal-weight arithmetic mean separately for V, A, and L. The fixed
V/A/L-to-market equation is then applied once to that shared state. There is no
special participant priority and no opposing hidden pressure channel.

## Current code map

- `components/c-val/2/mobile/index.tsx`: mandatory permission, automatic local
  zero, fixed orientation mapping, intermediate V/A/L, actual price feedback,
  and isolated temporary recording.
- `components/c-val/2/model/index.ts`: browser contracts and dormant snapshot.
- `components/c-val/2/transport/use-socket.ts`: version-owned browser socket.
- `socket/experiments/c-val/2/orientation.mjs`: direct calibration and angle
  wraparound.
- `socket/experiments/c-val/2/multi-user-control.mjs`: equal-weight V/A/L mean.
- `socket/experiments/c-val/2/model.mjs`: dormant lifecycle, fixed condition
  direction, agents, FIFO book, executions, and price.
- `socket/experiments/c-val/2/calibration.mjs`: C-VAL 1 market calibration.
- `socket/experiments/c-val/2/index.mjs`: room, aggregation, reset, snapshots,
  and temporary recorder transport.
- `components/c-val/2/controller/**`: market observation and temporary recorder
  command.
- `components/c-val/2/screen/**`: surrounding consequences only. Screen work
  must not alter mobile input, V/A/L equations, market direction, or sockets.

## Temporary recorder

The development-only recorder remains inactive unless the controller explicitly
starts it. While active it adds one raw orientation listener. Outside recording
it adds no sensor sampling work. It must be removed after real-phone validation,
including controller command, socket events, mobile listener, server writer,
types, and this section.

## Verification truth

Static tests currently establish only that:

- waiting state remains exactly empty at `100`;
- one fixed beta angle maps simultaneously to V/A/L;
- opposite beta-derived V/A/L states have opposite executed-price directions across
  multiple deterministic market paths;
- balanced angle is near zero and L damps rather than reverses direction;
- all prices remain execution-derived and the market conserves cash/inventory;
- V, A, and L retain distinct volatility, activity, and liquidity effects.

A deterministic three-second probe across six independent market seeds gave
these execution-derived results for the new fixed-axis trial:

- `beta -35°`: every run ended below `100`; mean end move `-34.24`, mean range
  `34.34`;
- `beta +35°`: every run ended above `100`; mean end move `+52.66`, mean range
  `52.80`;
- untouched `beta 0°`: the dormant market remained exactly `100`.

These numbers verify model magnitude and sign only. They do not satisfy the
human discoverability gate.

Static tests cannot establish whether a real person can discover the bodily
mapping quickly or whether its timing and magnitude feel right. That must be
judged on a real phone against the controller. The rejected implementations are
recorded separately in
[`2-interaction-failure-review.md`](./2-interaction-failure-review.md).
